const numero = 2;
const numero1 = 3

const array = [];

const añadir = array.push(numero);

const añadirInicio = array.unshift(numero1)

const index = array.at(1)

console.log(array)
console.log(index)
